// index.js
import { registerRootComponent } from 'expo';
import App from './src/App';

// registerRootComponent se encarga de registrar el componente principal y manejar el ciclo de vida de la app
registerRootComponent(App);
